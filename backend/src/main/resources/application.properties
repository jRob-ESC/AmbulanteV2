spring.application.name=Ambulante

# DATABASE CONFIGURATION
spring.datasource.url=jdbc:postgresql://ambulante_postgres:5432/${POSTGRES_DB}
spring.datasource.username=${POSTGRES_USER}
spring.datasource.password=${POSTGRES_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/HIBERNATE CONFIGURATION
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# FLYWAY CONFIGURATION
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true
spring.flyway.clean-disabled=false
spring.flyway.validate-on-migrate=false

server.address=0.0.0.0
server.port=8080

# SPRING SECURITY CONFIGURATION
spring.security.user.name=user

# IMAGES SERVER CONFIGURATION
app.upload.dir=/app/uploads
spring.web.resources.static-locations=classpath:/static/,file:/app/uploads